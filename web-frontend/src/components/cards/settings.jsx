import WeatherSettings from "../settings/WeatherSettings"

const Settings = ({ notify }) => {

    return (
        <>
            <h2>Asetukset</h2>
            <WeatherSettings />
        </>
    )   
}
export default Settings