{
  "name": "lifeline-app",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev-mobile": "npm-run-all --parallel mobile backend",
    "dev-desktop": "npm-run-all --parallel frontend backend",
    "backend": "cd backend && npm run dev",
    "mobile": "cd mobile-app && npm start",
    "frontend": "cd web-frontend && npm run dev"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "ssh2-sftp-client": "^12.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
