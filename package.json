{
  "name": "lifeline-app",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "mobile": "cross-env DEVICE=mobile npm-run-all --parallel mobile-launch backend",
    "desktop": "cross-env DEVICE=desktop npm-run-all --parallel frontend-launch backend",
    "backend": "cd backend && npm run dev",
    "mobile-launch": "cd mobile-app && npm start",
    "frontend-launch": "cd web-frontend && npm run dev"
  },
  "dependencies": {
    "apollo-server-express": "^3.13.0",
    "bcrypt": "^6.0.0",
    "express": "^5.1.0",
    "mongoose": "^8.18.2",
    "npm-run-all": "^4.1.5",
    "ssh2-sftp-client": "^12.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
